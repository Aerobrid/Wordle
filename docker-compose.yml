version: '3.8'

services:
  wordle:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: wordle-app
    restart: unless-stopped
    labels:
      - "com.wordle.description=Wordle Game - Flutter Web App"

